'use strict';

angular.module('app')
.component('loading', {
  templateUrl: 'app/loading/loading.html',
  controller: Controller,
  controllerAs: 'page'
});

Controller.$inject = [];

function Controller() {
}
